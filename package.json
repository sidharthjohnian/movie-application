{
  "name": "movie_app",
  "version": "1.0.0",
  "description": "Steps to use the application Download the zip file run the command - npm install you may need to run the command - npm install express run the command - node server.js Run the index.html file",
  "main": "server.js",
  "dependencies": {
    "express": "^4.17.1",
    "fs": "^0.0.1-security",
    "jsonwebtoken": "^8.5.1"
  },
  "devDependencies": {},
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js"
  },
  "author": "",
  "license": "ISC"
}
